# Capitalize the fist word

Write a function that acepts a string. The function should capitalize the first letter of each word in the string.

```
Example

capitalize("hello world") should return "Hello World"
```